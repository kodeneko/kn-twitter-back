import { DbErrorFilterFilter } from './db-error-filter.filter';

describe('DbErrorFilterFilter', () => {
  it('should be defined', () => {
    expect(new DbErrorFilterFilter()).toBeDefined();
  });
});
